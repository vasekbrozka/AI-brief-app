// How many past days the app keeps reachable: the Archive list, tappable
// "follows up on" links and the index the generator maintains all agree on it
// (docs/brief-generation.md, INDEX_MAX_DAYS in docs/check-brief.py).
export const ARCHIVE_DAYS = 14;
